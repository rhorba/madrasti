packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts unless explicitly approved here.
# argon2 is a native module and must build at install time (CLAUDE.md 16.4).
onlyBuiltDependencies:
  - argon2
  - sharp
  - esbuild
  - "@biomejs/biome"
